# Environment variables and secrets
.env
.env.*
!.env.example
*.key
*.pem
secrets.toml
.streamlit/secrets.toml

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
build/
dist/
*.egg-info/

# Jupyter
.ipynb_checkpoints/

# IDE and editor
.idea/
.vscode/
*.swp
*.swo

# Operating systems
.DS_Store
Thumbs.db
Desktop.ini

# Logs and local caches
*.log
.cache/
cache/
tmp/
temp/

# Local/private source materials
data/raw/
data/private/
*.pdf
*.xlsx~

# Generated model outputs may contain endpoint metadata.
# 仅放行以下「脱敏、可复核」的评测产物；其余一律忽略（含 .env、密钥、单次运行原始输出）。
# 这些放行文件由 `python -m eval.run_eval --offline --runs 3` 确定性生成，
# 不含任何 API Key / endpoint / 私有数据，供报告引用与人工复核。

# 在线（Hy3 真实调用）原始产物：本地留档、不入库。
# 仅作文档 §5 分析的原始凭证，repo 内 results/ 下放行的 7 个文件保持离线自检锚点。
results/online_local/
results/raw/*
!results/raw/.gitkeep
!results/raw/cases_run1.json
!results/raw/cases_run2.json
!results/raw/cases_run3.json

results/tables/*.json
!results/tables/stability.json
!results/tables/report_run1.json
!results/tables/report_run2.json
!results/tables/report_run3.json

# Streamlit secrets
.streamlit/
!.streamlit/config.toml

