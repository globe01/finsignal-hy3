[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers --basetemp=.pytest-tmp
filterwarnings =
    error::DeprecationWarning:app.*
    error::DeprecationWarning:eval.*
    error::DeprecationWarning:generator.*
